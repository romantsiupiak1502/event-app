import { View, ImageBackground } from 'react-native';

import { styled } from '../styles';

export const FlexView = styled(View)`
  flex: 1;
`;

export const FlexImageBackground = styled(ImageBackground)`
  flex: 1;
`;
