type ITheme = {
  colors: {
    primary: string,
    white: string,
    black: string;
  }
}

export const theme: ITheme = {
  colors: {
    primary: "#FFADB9",
    white: "#FFFFFF",
    black: "#000000"
  }
}
