type ITheme = {
  colors: {
    primary: string,
    white: string,
    black: string;
    rgbaBlack: string;
  }
}

export const theme: ITheme = {
  colors: {
    primary: "#FFADB9",
    white: "#FFFFFF",
    black: "#000000",
    rgbaBlack: "rgba(0, 0, 0, 0.7);"
  }
}
