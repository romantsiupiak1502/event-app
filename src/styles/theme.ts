type ITheme = {
  colors: {
    primary: string,
    white: string,
    black: string;
    gray: string;
    red: string;
  }
}

export const theme: ITheme = {
  colors: {
    primary: "#FF8200",
    white: "#FFFFFF",
    black: "#000000",
    gray: "#ABA9BB",
    red: "#FFF6F8",
  }
}
